{
  "entries": [
    {{- range .RegularPages }}
    {
      "term": "{{ trim (path.Base .Path) (path.Ext .Path) }}"
      {{- "," }}
      "url": "{{ (.OutputFormats.Get "JSON").Permalink }}"
    }
    {{-   if .Prev }}
    {{-     "," }}
    {{-   end }}
    {{- end }}
  ]
}